{
  "name": "infrasity-sales-sync",
  "version": "1.0.0",
  "description": "Auto-syncs Infrasity Google Sheet to salesData.js for the sales dashboard",
  "main": "scripts/sync.js",
  "scripts": {
    "dev":      "node scripts/dev.js",
    "sync":     "node scripts/sync.js",
    "sync:dry": "DRY_RUN=true node scripts/sync.js",
    "start":    "node scripts/scheduler.js"
  },
  "dependencies": {
    "node-cron": "^3.0.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
